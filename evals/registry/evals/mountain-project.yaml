mountain-project:
  id: mountain-project.dev.v0
  metrics: [accuracy]

mountain-project.dev.v0:
  # maps to evals/elsuite/basic/includes.py, class Includes
  # class: evals.elsuite.basic.includes:Includes
  class: evals.elsuite.modelgraded.classify:ModelBasedClassify
  args:
    # This samples.jsonl should contain all of the
    # fields in evals/registry/modelgraded/fact.yaml
    # using loss_logic/samples.jsonl as a template
    samples_jsonl: mountain-project/samples.jsonl
    eval_type: cot_classify
    modelgraded_spec: fact